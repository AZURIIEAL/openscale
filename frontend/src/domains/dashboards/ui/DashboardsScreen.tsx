import { ComingSoon } from '@/shared/design-system/ComingSoon';

export function DashboardsScreen() {
  return (
    <ComingSoon
      screenName="Dashboards"
      description="Custom charts and alerting rules built from any table or query. Needs the control-plane's query + alerting endpoints."
    />
  );
}
