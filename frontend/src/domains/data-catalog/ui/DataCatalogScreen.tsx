import { ComingSoon } from '@/shared/design-system/ComingSoon';

export function DataCatalogScreen() {
  return (
    <ComingSoon
      screenName="Data Catalog"
      description="Browse tables, schemas, lineage, and sample data across every layer. Needs the control-plane's catalog endpoints."
    />
  );
}
