import { ComingSoon } from '@/shared/design-system/ComingSoon';

export function MlWorkbenchScreen() {
  return (
    <ComingSoon
      screenName="ML Workbench"
      description="MLflow experiments, model registry, feature store browser. Needs the control-plane to proxy MLflow's tracking API."
    />
  );
}
