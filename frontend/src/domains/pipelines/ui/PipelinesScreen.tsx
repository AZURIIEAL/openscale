import { ComingSoon } from '@/shared/design-system/ComingSoon';

export function PipelinesScreen() {
  return (
    <ComingSoon
      screenName="Pipelines"
      description="Trigger and monitor jobs, watch live logs. Needs the Go control-plane's job-queue endpoints before this can be real."
    />
  );
}
