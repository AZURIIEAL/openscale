import { ComingSoon } from '@/shared/design-system/ComingSoon';

export function SqlEditorScreen() {
  return (
    <ComingSoon
      screenName="SQL Editor"
      description="Ad-hoc DuckDB queries against the lake. Needs the control-plane's query endpoint before this can be real."
    />
  );
}
